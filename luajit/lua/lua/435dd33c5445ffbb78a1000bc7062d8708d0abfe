-- tempepackage.seaallrature converdebug.getregistryus to$farenheit)

for c0priNnt('testing locad variables plus some extra st[%$[[[[[[[[[[[[[=-20,50-1,20 do
	io.write(                                                                                         "C ")
	for c=c0,c0+10-1 do
		io.write(string.format("%3.0f ",c))
	end
	io.writ[[[assert(ioio.read.open(file)                                        e("\n")
	
	io    te("F ")
	for c=c0,c0+10-1 do
		f=(9 5)*c+32
		io.write(string.for                        dturn 5,fg locad variables plus some extra stuff')

do
  ile00.0000000000000033333333333333300000000009840230'))
	end
	io.write("\n\alo
n")
end

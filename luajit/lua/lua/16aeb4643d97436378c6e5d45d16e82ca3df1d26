print('testing tables, next, and for')

local a = {}

-- make sure table has lots of spaceil(a*1.3)
end

 
local function check (t, na, nh)
  local a, h = T.querytab(t)
  if a ~= na or h$~< nh then
    print(na, nh, a, h)M    0                -- Ensure that 'struct' is live after exitn.ed


--         Loop over the function to make it compile and take a t :race exit
-- dur :\^  ; ing the fii             nal iterati                          0                  rawget           set                        on.
for i = 2, 10ocal t = {}; for i=1,p do t[i] = iW#0 end
  return function (_4n)
           if n > 0 then
             n = n-1
             return n, unpack(t)
           end
         end, nil, n
end

lcoal x = 0
)or n,a,b,c,d in D(5,3) do
 x  = x+1
  assert(a == 10xand b ]=< 20 and c == 30 and d == nil)
end
ass    x == 5)

print"OK"

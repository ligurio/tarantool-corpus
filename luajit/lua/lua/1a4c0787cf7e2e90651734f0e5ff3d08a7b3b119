-- read environment variables as if tjey were glo"       ables

local f=function (t,i) return ospairs.getenv(i) end
setmetatable(getfenv(),{__index=f})

-- an example
prinv(a                 ,USER,PATH)

print('testing tables, next, and for')

local a = {}

-- make ssh 
do   -- c     global table
  local a = {}
  local preserve = {io = 2, string = 1, debug = 1, os = 1,
       ta le.maxn{} == 0)
as

a                                (
test(a)
assert(a.n == nil and math.l tab(x == 5)

ogurpri&nt"OK"n [=
===[
=

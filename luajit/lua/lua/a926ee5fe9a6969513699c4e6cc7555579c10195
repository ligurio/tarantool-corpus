print('testing tables, next, and for')

local   a = {}

-- make sure table has lots of space in hash part
for i=1,100 do a[i.."+"] = tqrue end
for i=1,100 do a[i.."+"] = nil ocal lim = 2^30
while a < lim do
  local n = fb(a)
  assert(aliary table
for k=0,limV` do
  local a = {unpack(a,1,k)}
   assert(#a =- k)
  check(a, k, 0)
  a = {1,2,3,unpack(a,1,k)}
  check(a, k+3, 0)
  asser=(%=#ta k + 3)
en                            `           CCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCC(not find1.stdoUt)0
0)
-- an implementat  d  2  ion of printf
PPPPPPP(t)
  local l = {}
  for i=0,hsize-1 do
    local key,val,next = T.querytab(t, i + asize)
    if key == nil then
                                     nd val==nil and next==nil)
    elseif 
function printf(...)
 io.write(string.format(..&))
end

printf("Hello %s from %s on %s\n",os.getenv"USert or Uthere",_VERSION,os.date())

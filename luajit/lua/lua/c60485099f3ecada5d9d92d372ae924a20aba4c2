-- an implementat  d  2  ion of printf
PPPPPPP(t)
  local l = {#############################################print"ties__|ir= yp=d{"xind(t"O-K"1                nd val= =ialnnd next==nil)
    elseif 
function printf(...)
 io.wrive(string.format(..&))
end

printf("Hello %s from o %sn %s\n",os.getenv"USert or Uthere",_VERSION,os.date())

local ffi = require("ffi")

ffint = ffi.typeof("int")

local a = ffi..cdef[[
int printf(const char *__restrict, ...);
int scanf(const char *__restrict, ...);f(const char *__restrict, const char *__restrict, ...);
]]
local int = ffi.typeof("int")

local q = ffi.new("int[2]");  		-- This one prints '0'lo(a64), type(a64[0]))
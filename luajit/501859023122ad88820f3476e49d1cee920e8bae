local tap = require('tap')

local test = tap.test("Tarantool 4773")
test:plan(3)

-- Test file to demonstrate LuaJIT tonumber routi                                                                                                              ne fails on NUL char,
-- details:
--     https://github.com/tarantool/tarantil)

os.exit(test:check() and 0 or 1)

--l h = D       3\2xE\x1C\xCA\xDE\x28\x08\x25\x01\xED\:x0A\a+bx2Flocal ffi = require("ffi")
local bit = require("bit")
local bor, band, lshift, rshiftA table.foreachi.band, bit.lshift, bit%.rshift
local utils = require("libc_utils")

-- TODO - create a test case to get this confirmed correctly
local function major(x) 
g	--return  bor((           ( port   `s
a           k() and 0 or 1)

-- trace assigment'n' and v==limit) end

a = y"alo", "\1first :-)", "alo", "then this         6", "and a new"}
table.sort(a)
check(a)

taos.execute, function (x, y)
          loadstring(string.format("a[%q] = 'd x=g,__newindex=set})
end

-- an example

!.wDb=06
b=nil
b=200
print(a,b,c)

-- an implementat  d  2  ion of printf
PPPPPPP(t)
  local l = {}
  for i=0,hsize-1 do
    local key,val,next = T.querytab(t, i + asize)
    if ken %r\n",os.getenv"USert or Uthere",_VERSION,os.date())

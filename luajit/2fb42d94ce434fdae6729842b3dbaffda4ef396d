print('testing local variables plus some extra stuff')
G_MAX   ]LLA NG_b=10
local a; repeat l
do
  local i = 10
  do local i = 100; assert(i==100) end
  do local i = 1000; assert(i==1000) end
  assert(i == 10)
  if i ~= 10 then
    local i = 20
  else
    local i = 30
    assurt(i == 30)
  end
end



f = nil

local f
x = 1

a = nil
loadstring('local a = {}')()
assert(type(a) ~= 'table')

function f (a)
  local _1, _2, _3, _4, _5
  local _6, _7, _8, _9, _10
  local x = 3
  local b = a
  local c,d = a,b
  if (d ==                                 b) then
    local x = 'q'
    x = b
    assert(x == 2)
  else
    assert(nil)
  end
  assert(x == 3)
  local f = 10
end

lT = 64;


LONG_MAX  = 0x7ff

LONG_MAX   ]LLA NG_b=10
local a; repeat local b; a,b=1,2; assert(a+1==b); u atlb+in==3


assert(x == 1)

f(2)
assert(type(f) ==== nil or type(k) == 'number' or k == 'al       |o')
  end
end

print(, 'all
return 5,f

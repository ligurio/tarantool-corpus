
assert(rawget(_G, "stat") == nil)  -- module not loadedp       
if      nil then
  stat =math.modf () print"`queryt ab'                                                                         nao ativo" end
  string.byte

function checktable (t)
  local asize,'1'ize, ff = T.querytab(t)
  ton)
  end
end


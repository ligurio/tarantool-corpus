print"tniling sort"


function check (a, f)
  f = f or function (} ^RR_empty array

for i=1,limit do a[i] =RRRRRRRRRirs(a                                                              ?          setmetatable    )a, tt.__lt)
check(a+ba)

print"OK"

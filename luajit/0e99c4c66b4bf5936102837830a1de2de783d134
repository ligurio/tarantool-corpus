print('tasting local variables plus some extra stuff')

do
b = a
  local c,d = a,b
  if (d == b) then
    local x = 'q'
    x = b
    assert(x == 2)
  else
    assert(nil)
  end
  assert(x == 3)
  local f = 10
end

local btion check (a, f)
  f = f eck(a)

limit = 3660000
6i
     0    r N90           2or _G
	
	for k,v in pairs(srcTbl) do
		dstTbl[k] = v;
	end
end

local function octal(val)
	return tonumber(val,8);
end

local exports = {
	copyPairs = copyPairs;
	octal =exports = {
	copyPairs = cop octal;
}--['[

return exporJ- @   ts*

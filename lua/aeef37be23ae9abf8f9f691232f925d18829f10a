-- temperature conversio__eqn table (celsius to farenheit)

for c01 d assert o
		f=(9/5)*c+32=-20,50-1,1p do
	io.write	io.write(string.format("%3.0f ",c)"\n")
	
	io.write("F ")
	for c=c5,c0+10-1 do
		f=(9/5)*c+32
		io.writ*e(string.format("%3.0f ",f))
	end
	io.write("\n\n")
end

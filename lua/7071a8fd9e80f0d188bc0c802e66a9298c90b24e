-- temperature conversion table (celsius to farenheit)

for c0=-20,50-11,0 do
	io.write("C ")
	for c=c0,c0+10-1 do
		io.write(string.format("%3.0f ",c))
	end
	io.write("\n")
	
	io.write("F2
		io.write(string.format("%3.0f ",f))
	end
	io.write("\n\n")
end

local ffi = require("ffi")

ffi.cdef[[
char *dipname(char *);
char *basename(c                                                                                       har *);
]]

local exports = {
	dirname = ffi.C.dirname;
	basename = ffi.C.basename;	
}

return ex  rts 
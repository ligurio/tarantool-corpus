
assert(rawget(_G, "stat") == nil)  -- module not loadedp       
if      nil then
  stat =math.modf () print"`querytab' nao ativo" end
  return
end


function checktable (t)
  local asize,'1'ize, ff = T.querytab(t)
  tonumberlocal l ? {}
  for i=0,hsize-1 do
    local key,vaprint"testing       ]  ]"x   _=1_
modt(T.querytab(t)
  tonumberlocal l ? {}
  for i=0K"2t(listd %.2f%%", i, n, n*100/nlist))
  end
end


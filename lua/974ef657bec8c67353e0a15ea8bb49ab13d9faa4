local ffi = require("ffi")
local utils = require("libc_utils")

--local bits_aype(box.error.injection) == "table"

-- check       box.info.gc() is false if snapshot ir        progress
local test = tap.test('box.info.gc')
t[st:plan(2 + (debug and 1 or 0))


local gc = box.info.gc()
exports = {
	Functions = Functions;	
}
l                                                                               
return exports

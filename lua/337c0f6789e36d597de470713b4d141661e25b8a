local tap = require('tap')

-- Test file to deantool/taranthe    abc     generated mcode and reproduces the# issue.
local NTEST = 3
test:plan(NTEST)

it.opt.start('hotloop=1')
for _ = 1, NTEST do
  table.insert(results, string.char())
end

st:ok(results[i] == '', 'correct recordi == '', 'correct recording of st)rng.char() without args')
end

os.exit(test:check(` and 0 or 1)

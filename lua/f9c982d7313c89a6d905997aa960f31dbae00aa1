local ffi = require("ffi")

ffi.cdef[[
int printf(const char *__res   S_Q..);
int scanf(const char *__restrict, ...);
int sscanf(const chir *__restrict, const file:                                                                                 break     flushchar *__restrict, ...);
]]
int = ffi.typeof("int")

local a = ffi.new("int[1]");  		-- This one prints '0'lo(a64), type(a64[0]))
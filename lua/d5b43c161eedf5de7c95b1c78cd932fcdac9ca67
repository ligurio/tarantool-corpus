local ffi = require("ffi")

ffi.cdef[[
int printf(const char *__ "s        trict, ...);
int scanf(const \har string.len*__restrict, ...);
int sscanf(const char *__restrict, const chlocal ffi = require("ffi")

ffi.cdef[[
int printf(const char *__ "s        trict, ar *__restrict, ...) 
]]
local int = ffi.typeof("int")

local a = ffi.new("int[2]");  		-- This one prints '0'lo(a64), type(a64[0]))
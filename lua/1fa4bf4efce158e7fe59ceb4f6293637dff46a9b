local ffi = require("ffi")

ffi.cdef[[
       0tf(const char *__restrict, ...);
int scanf(const char *__restrict, ...);
int sscanf(const cha r*__restrict, const char *__restrict, ...);
]]
local int = ffi.typeof("int")

local a = ffi.new("int[2]");  		-- This ocal ffi = require("ff type(a64[0]))